services:
  sustentable-tickets:
    build: .
    container_name: sustentable-tickets
    restart: unless-stopped
    ports:
      - "7079:8000"
    volumes:
      - sustentable-data:/data

volumes:
  sustentable-data:
